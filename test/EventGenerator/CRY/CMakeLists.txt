#
# cosmic ray generation test;
# currently broken because of geometry issues, need to fix it
#
# cet_test(gencosmics HANDBUILT
#   TEST_EXEC lar
#   TEST_ARGS --rethrow-all --config gencosmics_test.fcl -n 2
#   DATAFILES gencosmics_test.fcl
#   OPTIONAL_GROUPS BROKEN
# )
